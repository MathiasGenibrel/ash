{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "default",
    "description": "Permissions de la fenêtre principale. Déclarer ce fichier remplace le jeu par défaut de Tauri : `core:default` doit donc y figurer explicitement.",
    "windows": ["main"],
    "permissions": ["core:default", "core:window:allow-maximize"]
}
